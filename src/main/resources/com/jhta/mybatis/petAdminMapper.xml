<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jhta.mybatis.petAdminMapper">

<select id="earn"  resultType="pay" parameterType="map">
	SELECT  sum(comn) as count, 
  to_char(pay_date, 'YYYY-MM-DD') as created_date 
FROM 
  pay 
WHERE 
<![CDATA[
  to_char(pay_date, 'YYYY-MM-DD')>=#{start} AND to_char(pay_date, 'YYYY-MM-DD')<#{end}
 ]]>
GROUP BY 
  to_char(pay_date, 'YYYY-MM-DD')
  order by created_date asc
</select>


</mapper>

